version: '2'

services:
  flasky:
    build: ./Flask
    ports:
      - "9001:9001"  # Port Flask is running on
    restart: always

  fnginx:
    build: ./nginx
    ports:
      - 8080:8080 # Port NGINX is listening on 


    restart: always

